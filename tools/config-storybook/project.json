{
  "name": "config-storybook",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "tools/config-storybook/src",
  "projectType": "library",
  "tags": ["scope:shared", "type:admin", "storybook", "config"],
  "targets": {
    "build": {
      "command": "cd tools/config-storybook; rm -fr dist/* && tsc -p tsconfig.lib.json && tsc -p tsconfig.cjs.json && ./fix-package.sh; cd -;"
    }
  }
}
